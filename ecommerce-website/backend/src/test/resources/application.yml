server:
  port: 8080

spring:
  application:
    name: ReSellMart
  profiles:
    active: test
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 25MB

  datasource:
    url: jdbc:mysql://localhost:3306/resellmart_db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
    database: mysql
    open-in-view: false

  flyway:
    enabled: false

  mail:
    host: localhost
    port: 1025
    username: user
    password: password
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

application:
  security:
    jwt:
      secret-key: ${TEST_JWT_SECRET_KEY:dGVzdC1qd3Qtc2VjcmV0LWtleS1mb3ItbG9jYWwtZGV2ZWxvcG1lbnQtb25seQ==}
      access-token:
        expiration: 3600000 # 1 hour
        #expiration: 60000 # 1 minute
      refresh-token:
        expiration: 604800000 # 1 week
        #expiration: 120000 # 2 minutes
    cors:
      allowed-origins: http://localhost:5173,http://localhost:4173

  backend:
    base-https-url: "https://fe25-2a00-23c8-820e-701-b0fe-b493-9617-40ea.ngrok-free.app"
  frontend:
    base-url: "http://localhost:5173"

  mailing:
    activation-url: "/auth/activation?code="

  stripe:
    secret-key: ${STRIPE_SECRET_KEY}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
    checkout-success-url: "/app/my-purchases"
    checkout-cancel-url: "/app/checkout"

  file:
    upload:
      product-images-path: ./test-uploads/product-images
      user-images-path: ./test-uploads/user-images
      order-item-images-path: ./test-uploads/order-item-images
